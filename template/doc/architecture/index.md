# <PROJECT_NAME> Architecture

System design documents: what the system is, structurally. Content lives in the per-topic documents below.

> **This file is a pointer list.** Do not put architecture content here — put it in a named subdocument and link it below. An index that accumulates content defeats the selective-loading model.

## Documents

- `overview.md` — high-level system shape, main modules, data flow (created by the Phase 1 bootstrap survey)
- `adr/` — architecture decision records (`adr/0000-template.md` for the format; list each ADR as it is written)
- <add documents as the architecture takes shape — one link per doc, with a one-line summary>
