# Tokens that look like a /cdd-* slash command or a repo file path but are
# intentionally NOT one. Consulted by scripts/prompt-seam-check.sh.
# Exact-line match; lines starting with # and blank lines are ignored.

# --- Not slash commands (check 1: command-name resolution) ---
# The sourced shell helpers (tools/cdd-worktree.sh, tools/cdd-state.sh),
# written without a leading slash precisely because they are not slash commands.
/cdd-worktree
/cdd-state
# Baseline marker file path (.claude/cdd-baseline), not a command.
/cdd-baseline
# Smoke-test temp dir (/tmp/cdd-demo-smoke in demo/README.md), not a command.
# The check runner provisions its own scratch dir, so /tmp/cdd-smoke is gone.
/cdd-demo-smoke
# Retired command name, kept only where it is recorded rather than used: the
# roadmap completion annotations documenting the /cdd-merge-main ->
# /cdd-merge-base rename (#31), and the legacy-token table in
# .claude/commands/cdd-retrofit.md section 4.5. Historical.
/cdd-merge-main

# --- Downstream paths absent from the CDD repo (check 3: path existence) ---
# Produced by /cdd-bootstrap in a downstream project; the CDD repo's own
# overview lives at doc/architecture/overview.md instead.
doc/knowledge_base/project-overview.md

# The pre-#73 knowledge-base index path, recorded in cdd-retrofit.md's section 4.4
# rename table and section 4.5 legacy-token table so already-bootstrapped projects
# are carried across the rename. Historical: the repo's own copy is
# doc/knowledge_base/index.md.
doc/knowledge_base/README.md
